class DevFestSpeaker {
 String id;
 String bio;
 String name;
 String company;
 String companyLogoUrl;
 String country;
 int order;
 String photoUrl;
 String shortBio;
 List sessions = List();
 List tags = List();
 List badges = List();
 String title;

  DevFestSpeaker(this.id, this.bio, this.name, this.company, this.companyLogoUrl, this.country, this.order, this.photoUrl, this.shortBio, this.sessions, this.tags, this.badges, this.title);
}
